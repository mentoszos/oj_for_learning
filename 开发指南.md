# 后端开发指南

> 欢迎加入 CodeCollab OJ 后端开发团队！本文档将帮助你快速上手项目开发。

## 📋 目录

- [项目概述](#项目概述)
- [环境准备](#环境准备)
- [项目结构](#项目结构)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [数据库设计](#数据库设计)
- [API 接口规范](#api-接口规范)
- [常见问题](#常见问题)

## 🎯 项目概述

CodeCollab OJ 是一个在线实时协作评测系统，后端使用 Spring Boot 3.x + Java 17 开发。

### 技术栈

- **框架**: Spring Boot 3.2.0
- **语言**: Java 17+
- **数据库**: MySQL 8.0
- **ORM**: MyBatis Plus 3.5.5
- **缓存**: Redis
- **消息队列**: RabbitMQ
- **实时通信**: WebSocket (STOMP)
- **构建工具**: Maven

## 🛠 环境准备

### 1. 安装 JDK

确保已安装 JDK 17 或更高版本：

```bash
java -version  # 应该 >= 17
```

### 2. 安装 Maven

确保已安装 Maven 3.6+：

```bash
mvn -version
```

### 3. 安装数据库和中间件

#### MySQL 8.0

```bash
# 创建数据库
CREATE DATABASE codecollab_oj CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 执行建表脚本
mysql -u root -p codecollab_oj < src/main/resources/db/mysql-docker-compose.yml
```

#### Redis

```bash
# 启动 Redis（默认端口 6379）
redis-server
```

#### RabbitMQ

```bash
# 启动 RabbitMQ（默认端口 5672）
rabbitmq-server
```

### 4. 配置项目

修改 `src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codecollab_oj?...
    username: root
    password: your_password
  
  data:
    redis:
      host: localhost
      port: 6379
  
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
```

### 5. 启动项目

```bash
cd backend
mvn spring-boot:run
```

或使用 IDE 直接运行 `CodeCollabOjApplication.java`

## 📁 项目结构

```
backend/
├── src/main/java/com/codecollab/oj/
│   ├── CodeCollabOjApplication.java  # 启动类
│   ├── config/                        # 配置类
│   │   ├── WebSocketConfig.java       # WebSocket 配置
│   │   ├── RabbitMQConfig.java        # RabbitMQ 配置
│   │   ├── MyBatisPlusConfig.java     # MyBatis Plus 配置
│   │   └── CorsConfig.java            # 跨域配置
│   ├── controller/                    # 控制器层
│   │   ├── ProblemController.java     # 题目控制器
│   │   └── SubmitController.java      # 提交控制器
│   ├── service/                       # 服务层
│   │   ├── QuestionService.java       # 题目服务接口
│   │   ├── JudgeService.java          # 判题服务接口
│   │   └── impl/                      # 服务实现
│   │       ├── QuestionServiceImpl.java
│   │       └── JudgeServiceImpl.java
│   ├── mapper/                        # 数据访问层
│   │   ├── QuestionMapper.java
│   │   └── QuestionSubmitMapper.java
│   ├── model/                         # 数据模型
│   │   ├── entity/                    # 实体类（对应数据库表）
│   │   │   ├── Question.java
│   │   │   ├── QuestionSubmit.java
│   │   │   └── InterviewRoom.java
│   │   ├── dto/                       # 数据传输对象（请求参数）
│   │   │   ├── SubmitRequest.java
│   │   │   └── ProblemQueryRequest.java
│   │   └── vo/                        # 视图对象（响应数据）
│   │       ├── ProblemVO.java
│   │       └── SubmitResultVO.java
│   └── common/                        # 通用类
│       └── BaseResponse.java          # 统一响应类
└── src/main/resources/
    ├── application.yml                # 配置文件
    └── db/
        └── mysql-docker-compose.yml                 # 数据库建表脚本
```

## 🚀 开发流程

### 1. 创建新功能模块

#### 步骤 1: 创建实体类

```java
// src/main/java/com/codecollab/oj/model/entity/NewEntity.java
@Data
@TableName("new_table")
public class NewEntity {
    @TableId(type = IdType.AUTO)
    private Long id;
    
    private String name;
    
    @TableField(value = "create_time", fill = FieldFill.INSERT)
    private LocalDateTime createTime;
}
```

#### 步骤 2: 创建 Mapper

```java
// src/main/java/com/codecollab/oj/mapper/NewMapper.java
@Mapper
public interface NewMapper extends BaseMapper<NewEntity> {
}
```

#### 步骤 3: 创建 Service

```java
// src/main/java/com/codecollab/oj/service/NewService.java
public interface NewService {
    NewEntity getById(Long id);
}

// src/main/java/com/codecollab/oj/service/impl/NewServiceImpl.java
@Service
public class NewServiceImpl implements NewService {
    @Autowired
    private NewMapper newMapper;
    
    @Override
    public NewEntity getById(Long id) {
        return newMapper.selectById(id);
    }
}
```

#### 步骤 4: 创建 Controller

```java
// src/main/java/com/codecollab/oj/controller/NewController.java
@RestController
@RequestMapping("/api/new")
@CrossOrigin(origins = "*")
public class NewController {
    @Autowired
    private NewService newService;
    
    @GetMapping("/{id}")
    public BaseResponse<NewEntity> getById(@PathVariable Long id) {
        NewEntity entity = newService.getById(id);
        return BaseResponse.success(entity);
    }
}
```

### 2. 数据库操作

#### 使用 MyBatis Plus

```java
// 查询
Question question = questionMapper.selectById(1L);

// 条件查询
QueryWrapper<Question> wrapper = new QueryWrapper<>();
wrapper.eq("id", 1).eq("is_delete", 0);
Question question = questionMapper.selectOne(wrapper);

// 插入
Question question = new Question();
question.setTitle("新题目");
questionMapper.insert(question);

// 更新
question.setTitle("修改后的标题");
questionMapper.updateById(question);

// 删除（逻辑删除）
questionMapper.deleteById(1L);
```

### 3. 使用 Redis

```java
@Autowired
private RedisTemplate<String, Object> redisTemplate;

// 设置
redisTemplate.opsForValue().set("key", "value", 60, TimeUnit.SECONDS);

// 获取
String value = (String) redisTemplate.opsForValue().get("key");

// 删除
redisTemplate.delete("key");
```

### 4. 使用 RabbitMQ

```java
@Autowired
private RabbitTemplate rabbitTemplate;

// 发送消息
rabbitTemplate.convertAndSend("queue_name", message);

// 接收消息（在消费者中）
@RabbitListener(queues = "queue_name")
public void handleMessage(String message) {
    // 处理消息
}
```

## 📝 代码规范

### 1. 命名规范

- **类名**: PascalCase，如 `QuestionService`
- **方法名**: camelCase，如 `getProblemById`
- **常量**: UPPER_SNAKE_CASE，如 `MAX_SIZE`
- **包名**: 小写，如 `com.codecollab.oj.service`

### 2. 代码组织

```java
@RestController
@RequestMapping("/api/example")
public class ExampleController {
    
    // 1. 依赖注入
    @Autowired
    private ExampleService exampleService;
    
    // 2. 公共方法
    @GetMapping("/{id}")
    public BaseResponse<ExampleVO> getById(@PathVariable Long id) {
        // 3. 参数校验
        if (id == null || id <= 0) {
            return BaseResponse.error("参数错误");
        }
        
        // 4. 业务逻辑
        ExampleVO vo = exampleService.getById(id);
        
        // 5. 返回结果
        return BaseResponse.success(vo);
    }
}
```

### 3. 注释规范

```java
/**
 * 获取题目详情
 * 
 * @param id 题目ID
 * @return 题目信息
 */
public ProblemVO getProblemById(Long id) {
    // 实现代码
}
```

### 4. 异常处理

```java
try {
    // 业务逻辑
} catch (Exception e) {
    log.error("操作失败", e);
    return BaseResponse.error("操作失败：" + e.getMessage());
}
```

## 🗄 数据库设计

### 表结构

#### question（题目表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| title | VARCHAR(512) | 标题 |
| content | TEXT | 题目内容（Markdown） |
| tags | VARCHAR(256) | 标签（JSON数组） |
| judge_config | TEXT | 判题配置（JSON） |
| judge_case | TEXT | 测试用例（JSON） |
| submit_num | INT | 总提交数 |
| accepted_num | INT | 通过数 |
| user_id | BIGINT | 创建人ID |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| is_delete | TINYINT | 逻辑删除 |

#### question_submit（提交记录表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| question_id | BIGINT | 题目ID |
| user_id | BIGINT | 提交者ID |
| language | VARCHAR(128) | 编程语言 |
| code | TEXT | 用户代码 |
| status | INT | 状态（0-待判题，1-判题中，2-成功，3-失败） |
| judge_info | TEXT | 判题结果（JSON） |
| create_time | DATETIME | 创建时间 |

### JSON 字段格式

#### judge_config

```json
{
  "timeLimit": 1000,
  "memoryLimit": 256
}
```

#### judge_case

```json
[
  {"input": "1 2", "output": "3"},
  {"input": "2 3", "output": "5"}
]
```

#### judge_info

```json
{
  "time": 100,
  "memory": 5000,
  "message": "Accepted"
}
```

## 🔌 API 接口规范

### 统一响应格式

```java
{
  "code": 200,        // 状态码：200-成功，500-失败
  "message": "success", // 消息
  "data": {}          // 数据
}
```

### 状态码说明

- `200`: 成功
- `400`: 参数错误
- `401`: 未授权
- `403`: 无权限
- `404`: 资源不存在
- `500`: 服务器错误

### 接口示例

#### GET /api/problem/{id}

**请求参数**:
- `id` (path): 题目ID

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "两数之和",
    "content": "...",
    "tags": ["数组", "哈希表"],
    "submitNum": 1200,
    "acceptedNum": 856,
    "judgeConfig": {
      "timeLimit": 1000,
      "memoryLimit": 256
    }
  }
}
```

#### POST /api/submit

**请求体**:
```json
{
  "problemId": 1,
  "code": "public class Solution {...}",
  "language": "java"
}
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "status": 0,
    "judgeInfo": null
  }
}
```

## 🐛 常见问题

### 1. 数据库连接失败

- 检查 MySQL 是否启动
- 检查 `application.yml` 中的数据库配置
- 检查数据库用户权限

### 2. Redis 连接失败

- 检查 Redis 是否启动
- 检查 `application.yml` 中的 Redis 配置
- 检查防火墙设置

### 3. RabbitMQ 连接失败

- 检查 RabbitMQ 是否启动
- 检查 `application.yml` 中的 RabbitMQ 配置
- 检查虚拟主机设置

### 4. MyBatis Plus 查询不到数据

- 检查实体类字段名是否与数据库字段名一致
- 使用 `@TableField` 注解映射字段
- 检查逻辑删除字段配置

### 5. 跨域问题

- 已配置 `CorsConfig`，确保前端请求包含正确的 Origin
- 检查 `@CrossOrigin` 注解配置

### 6. 端口被占用

修改 `application.yml` 中的端口：

```yaml
server:
  port: 8081  # 改为其他端口
```

## 📚 学习资源

- [Spring Boot 官方文档](https://spring.io/projects/spring-boot)
- [MyBatis Plus 文档](https://baomidou.com/)
- [Redis 官方文档](https://redis.io/docs/)
- [RabbitMQ 官方文档](https://www.rabbitmq.com/documentation.html)
- [WebSocket 文档](https://spring.io/guides/gs/messaging-stomp-websocket/)

## 🔗 相关文档

- [前端开发指南](../frontend/开发指南.md) - 了解前端接口调用
- [项目设计文档](../设计.md) - 了解整体架构设计
- [数据库设计文档](./README.md#数据库设计) - 了解数据库结构

## 💡 开发建议

1. **先理解现有代码**：在添加新功能前，先阅读相关代码
2. **遵循 RESTful 规范**：设计 API 时遵循 RESTful 风格
3. **做好异常处理**：所有可能出错的地方都要处理异常
4. **记录日志**：使用 `log.info()`、`log.error()` 记录关键操作
5. **编写注释**：复杂逻辑要写注释说明
6. **单元测试**：重要功能要编写单元测试
7. **代码审查**：提交前自己先审查一遍代码

## 🧪 测试

### 运行测试

```bash
mvn test
```

### 使用 Postman 测试 API

1. 导入 API 集合（如有）
2. 设置环境变量（baseUrl: http://localhost:8080）
3. 逐个测试接口

### 使用 curl 测试

```bash
# 获取题目
curl http://localhost:8080/api/problem/1

# 提交代码
curl -X POST http://localhost:8080/api/submit \
  -H "Content-Type: application/json" \
  -d '{"problemId":1,"code":"public class Solution{}","language":"java"}'
```

## 📞 联系方式

如有问题，请联系：
- 项目负责人：[姓名]
- 技术讨论群：[群号/链接]

---

**祝开发愉快！** 🎉

